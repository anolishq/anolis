schema_version: 1
machine_id: invalid-provider-ids-fixture
display_name: Invalid Provider ID Consistency Fixture

runtime_profiles:
  manual: ../../valid/runtime.manual.yml

providers:
  other0:
    config: ../../valid/provider0.yml

compatibility:
  runtime:
    config_contract: "01"
    http_contract: "02"
  providers:
    other0:
      strategy: local-build
      version: local
