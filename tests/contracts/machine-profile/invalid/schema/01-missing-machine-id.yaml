schema_version: 1
display_name: Invalid Fixture

runtime_profiles:
  manual: runtime.manual.yaml

providers:
  provider0:
    config: provider0.yaml

compatibility:
  runtime:
    config_contract: "01"
    http_contract: "02"
  providers:
    provider0:
      strategy: local-build
      version: local
