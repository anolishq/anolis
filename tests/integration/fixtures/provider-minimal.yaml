# Minimal provider configuration for lightweight tests
# Single temperature controller device

devices:
  - id: tempctl0
    type: tempctl
    initial_temp: 25.0

simulation:
  mode: non_interacting
  tick_rate_hz: 10
