# Minimal fixture. do_stage reads the components: block; variant selection reads
# runtime_profiles. Both variants are inert (no automation block).
runtime_profiles:
  manual: config/anolis-runtime.manual.yaml
  telemetry: config/anolis-runtime.telemetry.yaml

components:
  runtime:
    repo: anolishq/anolis
    version: "9.9.9"
  providers:
    foo:
      repo: anolishq/anolis-provider-foo
      version: "1.0.0"
    bar:
      repo: anolishq/anolis-provider-bar
      version: "2.0.0"
  optional:
    telemetry_export:
      version: "0.9.9"
